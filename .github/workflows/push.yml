name: Push workflow

on:
  push:
    branches:
      - '**'

permissions:
  contents: write

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: 1.25

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/.go/pkg/mod
          key: ${{ runner.os }}-go-mod-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-mod-

      - name: Check for dead code
        run: make lint

  test-eoas:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: apps/eoas

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      # 18 is the floor declared in the package's engines field.
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: apps/eoas/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type-check and build
        run: npm run build

      - name: Lint
        run: npm run lint

      - name: Run tests
        run: npm test

  test:
    runs-on: ubuntu-latest

    # The Postgres-backed store tests (users, SSO, progressive rollouts) cover SQL that
    # the in-memory fakes cannot reach: partial unique indexes, deferred FKs, guarded
    # UPDATEs and their behavior under concurrency. They skip themselves when
    # TEST_DATABASE_URL is unset, so without this service they never ran here and the
    # job went green having executed none of them.
    #
    # ClickHouse is here for the same reason and covers what no fake can either:
    # the embedded migrations applying twice, every analytical column matching what
    # the flattener binds to it, and the ASOF-joined health queries. Its own
    # migrations take a PostgreSQL advisory lock, so it needs both services. The
    # tests now FAIL rather than skip when CI is set, so a green job means they ran.
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: eoota_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      clickhouse:
        image: clickhouse/clickhouse-server:25.3
        env:
          CLICKHOUSE_DB: eoota_test
          CLICKHOUSE_USER: default
          CLICKHOUSE_PASSWORD: clickhouse
        ports:
          - 9000:9000
          - 8123:8123
        options: >-
          --ulimit nofile=262144:262144
          --health-cmd "wget --no-verbose --tries=1 --spider http://localhost:8123/ping || exit 1"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    env:
      TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/eoota_test?sslmode=disable
      TEST_CLICKHOUSE_URL: clickhouse://default:clickhouse@localhost:9000/eoota_test
      TEST_AZURE_BLOB_ENDPOINT: http://localhost:10000/devstoreaccount1

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: 1.25

      - name: Check if .env exists or create it
        run: |
          if [ ! -f .env ]; then
            touch .env
          fi

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/.go/pkg/mod
          key: ${{ runner.os }}-go-mod-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-mod-

      - name: Install Go dependencies
        run: |
          go mod tidy 
          go mod download

      # Not a service container: services cannot override the image command,
      # and Azurite needs --skipApiVersionCheck because the Go SDK speaks API
      # versions newer than the ones Azurite validates against.
      - name: Start Azurite (Azure Blob emulator)
        run: |
          docker run --name azurite -d -p 10000:10000 mcr.microsoft.com/azure-storage/azurite azurite-blob --blobHost 0.0.0.0 --skipApiVersionCheck
          for _ in {1..30}; do
            if (echo > /dev/tcp/127.0.0.1/10000) 2>/dev/null; then exit 0; fi
            sleep 1
          done
          echo "Azurite did not become ready" >&2
          docker logs azurite
          exit 1

      - name: Run tests
        run: make test_app html

      - name: Upload coverage artifact
        if: ${{ success() }}
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.html
          retention-days: 1
