apiVersion: v1
kind: Service
metadata:
  name: {{ include "xprem.fullname" . }}
  labels:
    {{- include "xprem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "xprem.selectorLabels" . | nindent 4 }}
