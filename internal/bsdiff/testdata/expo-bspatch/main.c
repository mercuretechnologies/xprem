/* Test harness: bspatch.c is the file expo-updates 56.0.23 vendors and runs on
 * the phone (expo-updates/vendor/bspatch/bspatch.c), copied unmodified. */
int bspatch_main(int argc, char *argv[]);

int main(int argc, char *argv[]) { return bspatch_main(argc, argv); }
