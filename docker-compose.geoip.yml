# Opt in with:
# docker compose -f docker-compose.yml -f docker-compose.geoip.yml up
services:
  ota-server:
    depends_on:
      geoipupdate:
        condition: service_healthy
    volumes:
      - geoip_data:/usr/share/GeoIP:ro
    environment:
      GEOIP_MMDB_PATH: /usr/share/GeoIP/GeoLite2-City.mmdb

  geoipupdate:
    image: ghcr.io/maxmind/geoipupdate:v7.1.1
    restart: unless-stopped
    environment:
      GEOIPUPDATE_ACCOUNT_ID: ${GEOIPUPDATE_ACCOUNT_ID:?Set GEOIPUPDATE_ACCOUNT_ID in .env}
      GEOIPUPDATE_LICENSE_KEY: ${GEOIPUPDATE_LICENSE_KEY:?Set GEOIPUPDATE_LICENSE_KEY in .env}
      GEOIPUPDATE_EDITION_IDS: GeoLite2-City
      GEOIPUPDATE_FREQUENCY: 72
    volumes:
      - geoip_data:/usr/share/GeoIP
    healthcheck:
      test: ["CMD-SHELL", "test -f /usr/share/GeoIP/GeoLite2-City.mmdb"]
      interval: 5s
      timeout: 2s
      retries: 12
      start_period: 5s

volumes:
  geoip_data:
